[project]
name = "diagnosis"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "strands-agents[a2a]>=1.40.0",
    "bedrock-agentcore>=1.9.1",
    "boto3>=1.34",
    "aws-opentelemetry-distro>=0.10.0",
    "httpx>=0.28",
    "common",
]

[tool.uv.sources]
common = { workspace = true }
