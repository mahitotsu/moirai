[project]
name = "infrastructure-inspector"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.0",
    "mcp[cli]>=1.0",
    "boto3>=1.34",
    "aws-opentelemetry-distro>=0.10.0",
]
